// Error -->
      // Programmer mistake (We did something wrong)
      // Calling a function with invalid arguments, IndexOutOfBound etc...
// Exception --> 
      // Failiure condition: something unexpected happened.
      // Unexpected: Out of controll.
      // Handle it (show message to user) and recover.